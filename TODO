TODO:
    * drag and drop of filenames
    * save folder and window size across sessions
    * fix popup menu not being shown on first click

DONE:
    * remove all methods other than locate
    * optionally intersect (AND) all search terms instead of ORing them
    * remove suggestions
    * rename "exact" to "match case"
